# 子项 D 真实 CLIP 启用 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** 真实 CLIP 打通验证：@live 向量冒烟 + 构建配置静态核对 + 启用文档。代码已完整（ChineseClipEmbedder / env 驱动 EMBEDDER=clip / worker INSTALL_ML 构建），本项不改功能代码。

**Architecture:** 保持 env 驱动（EMBEDDER=clip + worker INSTALL_ML=true）。加 @live 冒烟测试（默认跳过，用户 [ml] 环境跑）+ 一个静态构建配置核对测试（锁定 pyproject/Dockerfile/compose）+ 文档。

**Tech Stack:** pytest / tomllib(py3.11 stdlib) / Docker / cn_clip(ml, 用户环境)。

## Global Constraints

- Python 3.11；`.venv/bin/python -m pytest`（从 `server/`）。不用系统 python3。
- **pytest 0 warnings**；不装 torch/[ml]（本环境），clip 模块懒加载可 import；真实向量走 `@pytest.mark.live` 默认跳过。
- 不改功能代码（ChineseClipEmbedder/工厂/worker 现状已正确）。
- 文档准确：命令/flag 必须匹配真实 Dockerfile/compose/pyproject。

---

### Task 1: @live 向量冒烟 + 构建配置静态核对测试

**Files:**
- Create: `server/tests/test_live_clip.py`（@live 默认跳过）
- Create: `server/tests/test_build_config.py`（静态核对 pyproject/Dockerfile/compose）
- Test: 上述 + 全量回归

**Interfaces:** 无生产代码改动；仅测试。

- [ ] **Step 1: 写测试** `server/tests/test_live_clip.py`
```python
"""真实 CLIP 向量冒烟(@live 默认跳过)。需先装 [ml]（torch/cn_clip，数 GB）。跑法：
  .venv/bin/pip install -e '.[ml]'   # 用户 [ml] 环境
  .venv/bin/python -m pytest tests/test_live_clip.py -m live -v"""
import math
import pytest

_IMG = "https://ir.ozone.ru/s3/multimedia-1-3/7473007263.jpg"   # 一张公开 Ozon 商品图


@pytest.mark.live
@pytest.mark.asyncio
async def test_live_clip_embed_512_normalized():
    try:
        from app.services.embedding.clip import ChineseClipEmbedder
        emb = ChineseClipEmbedder()
        vec = await emb.embed_image(_IMG)
    except ImportError:
        pytest.skip("需安装 [ml]（torch/cn_clip）")
    assert isinstance(vec, list) and len(vec) == 512
    assert all(isinstance(x, float) for x in vec)
    norm = math.sqrt(sum(x * x for x in vec))
    assert 0.9 < norm < 1.1        # L2 归一化, 模长≈1
```

- [ ] **Step 2: 写测试** `server/tests/test_build_config.py`（静态核对构建路径；用 tomllib 读 pyproject，文本读 Dockerfile/compose）
```python
"""构建配置核对：确保真实 CLIP 的 [ml] 依赖 + INSTALL_ML 构建路径 + compose worker 参数正确。"""
import pathlib
import tomllib

_SERVER = pathlib.Path(__file__).resolve().parents[1]      # ozon-listing-auto/server
_ROOT = _SERVER.parent                                     # ozon-listing-auto


def test_pyproject_ml_extra_has_torch_and_cnclip():
    data = tomllib.loads((_SERVER / "pyproject.toml").read_text())
    ml = data["project"]["optional-dependencies"]["ml"]
    joined = " ".join(ml).lower()
    assert "torch" in joined and "cn-clip" in joined       # 真实 CLIP 依赖


def test_dockerfile_install_ml_branch():
    df = (_SERVER / "Dockerfile").read_text()
    assert "INSTALL_ML" in df and ".[dev,ml]" in df        # INSTALL_ML=true 装 [ml]


def test_compose_worker_has_install_ml_and_embedder():
    dc = (_ROOT / "docker-compose.yml").read_text()
    assert "INSTALL_ML" in dc and "EMBEDDER" in dc          # worker 传构建 arg + env
```

- [ ] **Step 3: 运行确认**
`.venv/bin/python -m pytest tests/test_live_clip.py tests/test_build_config.py -q` —— build_config 3 个通过；live 1 个 deselected（默认 `-m "not live"`）。若 build_config 某断言失败，说明现状与预期不符——**不要改测试迁就**，报告实际情况（现状应通过，因构建配置已正确）。

- [ ] **Step 4: 全量回归**
`.venv/bin/python -m pytest tests -q` —— 全绿 0 warnings（live deselected 数 +1）。

- [ ] **Step 5: 提交**
```bash
git add server/tests/test_live_clip.py server/tests/test_build_config.py
git commit -m "test(clip): @live CLIP 向量冒烟(512 维归一化) + 构建配置静态核对([ml]/INSTALL_ML/compose)"
```

---

### Task 2: 文档 + 回归

**Files:**
- Create: `docs/真实CLIP启用说明.md`
- Modify: `README.md`（真实 CLIP 小节）
- Modify: `docs/去mock化-真实集成总规划.md`（标 D 完成）
- Test: 全量回归

- [ ] **Step 1: 读现有** `README.md`（embedder/CLIP 提及处，M1/M2 说明可能已有 mock/clip 开关描述）。

- [ ] **Step 2: 建 `docs/真实CLIP启用说明.md`** —— 内容（准确对齐 Dockerfile/compose/pyproject）：
  - 目的：货源匹配跨平台图片去重的真实向量（cn_clip ViT-B/16 CPU）。
  - 启用步骤：`.env` 设 `INSTALL_ML=true` + `EMBEDDER=clip` → `docker compose up -d --build worker`（重建 worker 装 torch/cn_clip，镜像数 GB）。
  - 资源占用：worker 镜像数 GB；运行内存（加载模型）；**首次加载会下载 ViT-B/16 权重**（需外网/预热）。
  - CPU 推理取舍：慢于 GPU，但匹配在 worker 异步、不阻塞 API；大批量可分批/续跑（现有断点续传）。
  - 验证：`EMBEDDER=clip` 起 worker 后跑一个货源匹配任务，看 supply_candidates.embedding 有 512 维向量 + 跨平台去重分组；或跑 `@live` 向量冒烟。
  - 注意：`EMBEDDER=clip` 与 `INSTALL_ML=true` **须同时设**，否则 worker 首次 embed 报 ImportError（懒加载）。回退：`EMBEDDER=mock` 恢复轻量。

- [ ] **Step 3: 更新 `README.md`** —— 「真实 CLIP」小节（或补充现有 embedder 说明）：一句话 + 指向《真实CLIP启用说明》；`@live` 跑法。保持精简、与代码一致。

- [ ] **Step 4: 更新 `docs/去mock化-真实集成总规划.md`** —— 子项 D 标「已完成（@live 冒烟 + 构建核对 + 文档）」（定点标注）。

- [ ] **Step 5: 全量回归**
```bash
cd server && .venv/bin/python -m pytest tests -q
cd ../web && source ~/.nvm/nvm.sh && nvm use 20 >/dev/null && npx vitest run && npm run build
```
Expected: 后端全绿 0 warnings；前端通过 + build（前端本项无改动，回归确认不破坏）。

- [ ] **Step 6: 提交**
```bash
git add docs/真实CLIP启用说明.md README.md docs/去mock化-真实集成总规划.md
git commit -m "docs(clip): 真实 CLIP 启用说明 + README + 总规划标 D 完成"
```

---

## Self-Review

- **Spec 覆盖**：§2.1 @live 冒烟→Task 1；构建核对→Task 1；§2.2 文档→Task 2；验收→全量。全覆盖。无 UI 切换（用户确认 env 驱动）。
- **占位符扫描**：无 TBD；测试代码完整；文档 Task 2 以要点清单给出（对齐真实 Dockerfile/compose）。
- **落地注意**：不装 torch，clip 懒加载可 import（现状）；build_config 测试用 py3.11 stdlib `tomllib`；@live 用 ImportError→skip 双保险；不改任何功能代码——若发现须改功能代码才能通过，说明现状有问题，报告而非强改。
