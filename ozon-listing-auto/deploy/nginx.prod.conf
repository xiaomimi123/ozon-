# 生产 nginx：80 强制跳 443 + ACME 挑战；443 TLS 托管前端静态 + 反代 /api /ws。
# 作为 /etc/nginx/templates/default.conf.template 挂载，nginx 官方镜像启动时 envsubst 渲染 ${DOMAIN}。
#
# 注意（TLS 引导顺序）：下面 443 server 的 ssl_certificate 要求证书已存在，
# 否则 nginx 启动即失败。首次证书须先用 deploy/certbot-init.sh（--standalone，
# 不依赖 nginx）签发，再启动 nginx。80 端口这个 /.well-known/acme-challenge/
# 位置只用于 nginx 已启动之后的自动续期（webroot），不参与首次签发。
server {
    listen 80;
    server_name ${DOMAIN};
    location /.well-known/acme-challenge/ { root /var/www/certbot; }
    location / { return 301 https://$host$request_uri; }
}

server {
    listen 443 ssl;
    server_name ${DOMAIN};

    ssl_certificate     /etc/letsencrypt/live/${DOMAIN}/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/${DOMAIN}/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    add_header Strict-Transport-Security "max-age=63072000" always;
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options SAMEORIGIN always;
    add_header Referrer-Policy strict-origin-when-cross-origin always;

    client_max_body_size 20m;

    location /api/ {
        proxy_pass http://api:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    location /ws/ {
        proxy_pass http://api:8000/ws/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;
    }
}
